{
  "name": "css-nodeserver-v1",
  "version": "1.0.0",
  "description": "Production Ready MEAN Stack Base Template V1 - Node API Server",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:env": "node  -r dotenv/config  server.js"
  },
  "author": "Gauri",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.9.4"
  },
  "devDependencies": {
    "dotenv": "^8.2.0"
  }
}
